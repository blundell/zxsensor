apply plugin: 'com.android.library'
apply plugin: 'com.novoda.bintray-release'

buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.novoda:bintray-release:0.7.0'
    }
}

android {
    compileSdkVersion 27
    buildToolsVersion "27.0.3"
    defaultConfig {
        minSdkVersion 27
        targetSdkVersion 27
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
}

dependencies {
    provided 'com.google.android.things:androidthings:0.7-devpreview'
    compile 'com.android.support:support-annotations:27.1.0'

    testCompile 'junit:junit:4.12'
    testCompile 'org.mockito:mockito-core:2.2.25'
}

publish {
    userOrg = 'blundell'
    groupId = 'com.blundell'
    artifactId = 'driver-zxsensor'
    publishVersion = '0.0.2'
    desc = 'ZX Gesture Sensor driver library for AndroidThings'
    website = 'https://github.com/blundell/zxsensor'
}
